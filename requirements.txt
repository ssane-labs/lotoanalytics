# Сборка данных: нейросеть учится на numpy.
numpy>=2.0

# Только для scripts/calibrate_popularity.py — обучения модели популярности на
# итогах тиражей. Запускается вручную, раз в несколько месяцев; в ежедневной
# сборке и тестах не нужен:
#   pip install torch scipy
