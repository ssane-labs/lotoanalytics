# Ядро работает на чистой стандартной библиотеке Python 3.11+ —
# ни одна зависимость ниже не обязательна для сборки данных.

# Опционально: включает стратегию «градиентный бустинг» в бэктесте
# scripts/verify_randomness.py. Без неё скрипт просто пропускает эту модель.
scikit-learn>=1.4; python_version >= "3.9"
