/**
 * Настройки, которые меняются без правки кода.
 *
 * Пока адрес пустой, соответствующий блок интерфейса просто не показывается —
 * это лучше, чем кнопка, ведущая в никуда.
 */

export const CONFIG = {
  /**
   * Адрес воркера с платежами: https://<имя>.<поддомен>.workers.dev
   * Пока пусто — блок подписки показывает «скоро», покупка недоступна.
   */
  WORKER_URL: '',

  /**
   * Страница Boosty для донатов, например https://boosty.to/username
   * Пока пусто — кнопка доната скрыта.
   */
  DONATE_URL: '',

  /** Сколько комбинаций в день доступно без подписки. */
  FREE_DAILY_LIMIT: 1,
};
