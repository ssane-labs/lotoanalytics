/**
 * Настройки, которые меняются без правки кода.
 *
 * Пока адрес пустой, соответствующий блок интерфейса просто не показывается —
 * это лучше, чем кнопка, ведущая в никуда.
 */

export const CONFIG = {
  /**
   * Адрес воркера с платежами. Пусто — блок подписки не показывается.
   * Без слэша на конце: пути дописываются как '/api/...'.
   */
  WORKER_URL: 'https://loto-analytics-bot.shevelevandrei2000.workers.dev',

  /**
   * Страница Boosty для донатов. Пусто — кнопка доната скрыта.
   *
   * Это ссылка наружу, а не покупка внутри приложения, поэтому правило
   * Telegram о Stars её не касается: за донат ничего не выдаётся.
   */
  DONATE_URL: 'https://boosty.to/fabrix.com/donate',

  /** Сколько комбинаций в день доступно без подписки. */
  FREE_DAILY_LIMIT: 1,
};
