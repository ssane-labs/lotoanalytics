/**
 * Настройки, которые меняются без правки кода.
 *
 * Пока адрес пустой, соответствующий блок интерфейса просто не показывается —
 * это лучше, чем кнопка, ведущая в никуда.
 */

export const CONFIG = {
  /**
   * Адрес воркера с кошельком и оплатой. Пусто — приложение работает целиком
   * в браузере: одна бесплатная прокрутка в день, без рекламы и покупок.
   * Без слэша на конце: пути дописываются как '/api/...'.
   */
  WORKER_URL: 'https://loto-analytics-bot.shevelevandrei2000.workers.dev',

  /**
   * Страница Boosty для донатов. Пусто — кнопка доната скрыта.
   *
   * Это ссылка наружу, а не покупка внутри приложения, поэтому правило
   * Telegram о Stars её не касается: за донат ничего не выдаётся.
   */
  DONATE_URL: 'https://boosty.to/fabrix.com/donate',

  /**
   * Рекламный блок Adsgram (rewarded video для Telegram Mini App).
   *
   * Значение приезжает с воркера — переменной ADSGRAM_BLOCK_ID в панели
   * Cloudflare, чтобы менять его без пересборки приложения. Строка ниже —
   * запасной вариант на случай, если воркер недоступен. Пусто в обоих
   * местах — кнопка «Смотреть рекламу» не показывается совсем.
   */
  ADSGRAM_BLOCK_ID: '',

  /**
   * Экономика по умолчанию. Настоящие значения приходят с воркера вместе с
   * балансом — здесь они нужны, чтобы интерфейс не был пустым до ответа
   * сервера и чтобы приложение считало то же самое вне Telegram.
   */
  ECONOMY: {
    /** Прокруток даём бесплатно каждый день. */
    FREE_PER_DAY: 1,
    /** Сколько прокруток можно получить за рекламу в течение недели. */
    ADS_PER_WEEK: 5,
    /** Прокруток за один просмотр рекламы. */
    AD_REWARD: 1,
    /** Одна подобранная комбинация — одна прокрутка. */
    COST_PER_TICKET: 1,
    /** Разбор своей комбинации. */
    COST_CHECK: 2,
  },

  /**
   * Пакеты прокруток — запасной список для показа цен вне Telegram.
   * Покупка возможна только внутри Telegram, там список приходит с воркера.
   */
  PACKS: [
    { key: 'p1', spins: 1, stars: 30 },
    { key: 'p5', spins: 5, stars: 125 },
    { key: 'p10', spins: 10, stars: 200 },
  ],
};
